
- 2026-05-01: The repository was greenfield for implementation work, with no existing Cargo or Rust module structure. The scaffold had to establish the initial package layout while still avoiding premature NIH-plug or host-integration scope.
- 2026-05-01: The container lacked both `rust-analyzer` and the default GNU linker toolchain (`cc` plus system libs), so verification had to fall back to compiler checks and MUSL-target Cargo runs/tests using the bundled Rust linker.
- 2026-05-01: `lsp_diagnostics` still could not initialize for Rust because `rust-analyzer` is unavailable in the toolchain, so this resonator task relied on passing `cargo test` and `cargo run` for compile/runtime verification after the renderer change.
- 2026-05-01: Rust diagnostics remain unavailable for the tank-profile step too; `lsp_diagnostics` timed out during initialize because `rust-analyzer` is not installed, so verification again had to rely on successful `cargo test` and `cargo run` results.
- 2026-05-01: Rust diagnostics were still unavailable for the family-tuning step because `rust-analyzer` is not installed, so the updated modal tables and differentiation tests had to be verified through `cargo test` and `cargo run` rather than LSP diagnostics.
- 2026-05-01: Rust diagnostics were still unavailable for the size-scaling step because `rust-analyzer` is not installed, so verification again depended on passing `cargo test` and `cargo run` instead of `lsp_diagnostics`.
- 2026-05-01: Rust diagnostics remained unavailable for the rust-decay prototype too because `rust-analyzer` is still not installed; the `lsp_diagnostics` initialize request timed out again, so verification for this step relied on successful `cargo fmt`, `cargo test`, and `cargo run` results.
- 2026-05-01: Rust diagnostics were still unavailable for the damage prototype because `rust-analyzer` is not installed, so verification again had to rely on `cargo fmt`, `cargo test`, and `cargo run`. During testing, raw zero-crossing counts and simple envelope-flux roughness checks proved too brittle for the detuned-beating pipe render, so the render-side assertion was switched to a zero-crossing interval-variation proxy instead.
- 2026-05-01: The current damage transform doubles the mode bank at full intensity, which raises the offline peak path to 12 modes for pipe and 16 for plate/tank. That makes full-damage mode counts a poor first real-time default until the planned CPU hot-spot task establishes how much companion-mode headroom is actually affordable.
- 2026-05-01: The hotspot review confirmed one concrete later-plugin risk in the current prototype: `ModalModeSpec::damaged` allocates a fresh `Vec` per source mode during `scaled_mode_specs`, and `PlaceholderResonator::from_profile` then allocates another `Vec<SecondOrderMode>` from the expanded results. That overhead is acceptable for the offline prototype, but it should not migrate unchanged into any future real-time parameter-rebuild path.
- 2026-05-01: Rust diagnostics were still unavailable for the family-comparison render step because `rust-analyzer` is not installed in this environment; `lsp_diagnostics` timed out during initialize again, so verification for the new comparison-artifact path relied on successful `cargo fmt`, `cargo run`, and `cargo test` results.
- 2026-05-01: Rust diagnostics remained unavailable for the rust-variation render step as well because `rust-analyzer` is still missing; `lsp_diagnostics` timed out on both `src/main.rs` and `src/renderer.rs`, so final verification again depended on `cargo fmt`, `cargo run`, and `cargo test` instead.

- 2026-05-01: Rust diagnostics remained unavailable for the damage-variation render step too because `rust-analyzer` is still missing in this environment; `lsp_diagnostics` timed out again on both `src/main.rs` and `src/renderer.rs`, so final verification for the new damage artifacts had to rely on passing `cargo run` and `cargo test` instead.
