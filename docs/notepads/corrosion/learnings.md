
- 2026-05-01: A minimal offline renderer scaffold works best when the CLI entry point stays separate from the DSP harness. Keeping the render loop and summary generation in `src/renderer.rs` makes later resonator, object-profile, and comparison work additive instead of forcing a main-file rewrite.
- 2026-05-01: For this environment, the cleanest project-local Rust build strategy is a Cargo config that sets the MUSL target, the bundled `rust-lld` linker, and an external `target-dir`. That keeps plain Cargo commands working while preventing generated build output from reappearing inside the repo tree.
- 2026-05-01: A modal prototype can stay deterministic and profile-ready by representing each mode as a cached second-order resonator whose `a1` and `a2` are regenerated from frequency, decay seconds, and sample rate while the offline harness continues to drive a single impulse excitation.
- 2026-05-01: Making the offline renderer build a concrete `ExcitationInput` before sample processing is a clean way to keep excitation deterministic while also giving later comparison renders and object-profile tests a reusable input fixture instead of duplicating impulse logic in each loop.
- 2026-05-01: The cleanest way to add the first object family without disturbing the deterministic renderer is to express it as a curated modal profile table that instantiates the same second-order mode bank. That keeps the hollow/tubular pipe direction in data while preserving one shared excitation and resonator path.
- 2026-05-01: Adding a second object family cleanly works best when profile identity, curated mode tables, and resonator construction stay in the same shared lookup path. That lets plate-specific tests prove a distinct metallic/inharmonic character without touching the deterministic excitation flow or adding a separate render branch.
- 2026-05-01: A tank-style object can join the same modal-profile seam cleanly when its boomier identity is expressed as lower fundamentals, longer low-mode decays, and stronger low-band gains in the curated table. That keeps the cavity-like metallic direction in data and makes later family tuning a matter of profile refinement rather than renderer branching.
- 2026-05-01: Family-differentiation tests are more reliable in this offline modal prototype when they compare normalized behavior metrics—such as zero-crossing density, level-normalized waveform motion, and late-to-early energy ratio—rather than relying on checksum differences alone, because those metrics track perceptual brightness and sustain while preserving the shared resonator path.
- 2026-05-01: Size is easiest to integrate into the offline modal prototype when each family's static mode table stays canonical and a shared scaling transform derives lower/higher frequencies plus longer/shorter decays before resonator construction. That keeps size behavior additive across pipe, plate, and tank while letting the same render metrics verify brighter/tighter versus larger/looser directions.
- 2026-05-01: Rust fits the offline modal prototype cleanly when it is another shared pre-resonator transform on the mode specs: keeping modal frequencies intact while shortening decays and attenuating higher modes more strongly gives a deterministic, testable duller/shorter result without breaking the existing family or size architecture.
- 2026-05-01: Damage fits the same offline modal seam best when it expands each transformed base mode into a deterministic detuned companion pair before second-order construction. That keeps instability/roughness data-driven across families and makes render verification more reliable when it watches zero-crossing interval variability instead of a raw crossing count alone.
- 2026-05-01: A safe real-time mode-count starting point can stay fully grounded in the current profile architecture by using each family's canonical table as the conservative per-voice budget—6 modes for pipe, 8 for plate, and 8 for tank—while treating damage-spawned companion modes as an offline or later-optimization layer rather than the first real-time default.
- 2026-05-01: The current offline renderer's dominant CPU cost is structurally clear from `src/renderer.rs`: `OfflineRenderer::render_with_excitation` walks every frame while `PlaceholderResonator::process_sample` walks every active mode, so the steady-state hot path is the nested frame × mode loop ending in `SecondOrderMode::process`. At the current defaults that means 48,000 per-frame summary updates plus 6-16 second-order mode updates per frame, making per-mode sample processing the first place to budget for later real-time/plugin work.
- 2026-05-01: The cleanest way to produce Gate 0 family comparison evidence without destabilizing the prototype is to keep artifact generation inside `OfflineRenderer` and drive it from the existing shared profile constructors. Writing one WAV plus one summary per family under `output/family-comparisons/` keeps the renderer deterministic while making pipe/plate/tank inspection straightforward later.
- 2026-05-01: Low/high rust evidence stays easy to inspect when the offline renderer writes a dedicated `output/rust-variations/` manifest plus per-variant summaries that include both the normal render stats and behavior proxies such as `brightness_proxy` and `late_to_early_energy_ratio`. Reusing the shared `with_profile_size_and_rust(...)` path keeps those text metrics aligned with the actual WAV output instead of inventing a separate analysis branch.

- 2026-05-01: Low/high damage evidence is easiest to inspect when it follows the same dedicated-folder pattern as the earlier family and rust exports: a `output/damage-variations/` manifest plus per-variant summaries that include the normal render stats and roughness/decay proxies. Reusing `with_profile_size_rust_and_damage(...)` keeps the offline evidence aligned with the shared damage transform instead of introducing a damage-only render path.
