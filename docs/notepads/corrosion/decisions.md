
- 2026-05-01: Chose a Rust CLI scaffold with a `ResonatorCore` trait and a deterministic placeholder resonator. This keeps Gate 0 focused on offline rendering while providing a direct integration seam for the later modal resonator without introducing plugin shell code or extra dependencies.
- 2026-05-01: Added project-local Cargo defaults in `.cargo/config.toml` rather than modifying shell environment or global Rust config. The config pins builds to `x86_64-unknown-linux-musl`, uses the installed `rust-lld`, enables self-contained linking, and redirects build artifacts to `../corrotion-target` so plain `cargo run` / `cargo test` work from the repo root without polluting the working tree.
- 2026-05-01: Kept the existing `PlaceholderResonator` public seam but replaced its internals with a bank of second-order modes in `src/renderer.rs`. That preserves the current CLI/offline-render entry path while making the implementation directly reusable for later pipe, plate, and tank mode tables.
- 2026-05-01: Promoted the renderer's impulse excitation into a dedicated `ExcitationInput` abstraction with `deterministic_excitation()` and `render_with_excitation(...)`. This keeps the default render path unchanged for the CLI while exposing a first-class, repeatable excitation source for later A/B render comparisons and profile tests.
- 2026-05-01: Represented the new pipe object as `ModalProfile::pipe()` plus a `ModalProfileId` tag instead of introducing a pipe-specific render branch. The default resonator now selects that profile and dispatches through the same summed second-order mode path, leaving a straightforward extension seam for later plate and tank profiles.
- 2026-05-01: Added the initial plate object as a second curated `ModalProfile` selected through the same `ModalProfileId` lookup and `PlaceholderResonator::with_profile(...)` constructor. Keeping the new profile data-only avoids premature CLI/object-selection work while still exposing a clean extension seam for later tank integration and cross-object tuning.
- 2026-05-01: Added the initial tank object as a third curated `ModalProfile` selected through the existing `ModalProfileId` lookup and `PlaceholderResonator::with_profile(...)` constructor. The tank table emphasizes lower, longer-lived modes and is verified through the same deterministic render path so later cross-object tuning can stay data-driven instead of adding family-specific DSP branches.
- 2026-05-01: Tuned the existing pipe, plate, and tank tables by pushing the separation further in data only: pipe stays the most harmonic with longer ringing upper partials, plate shifts toward shorter-lived inharmonic high modes, and tank leans harder into low clustered modes with the longest decay. The renderer still uses the same shared second-order mode bank and deterministic excitation path for all families.
- 2026-05-01: Implemented size as a shared `SizeScale` transform applied to profile mode specs before `PlaceholderResonator` builds its second-order bank. Larger scales lower modal frequencies, lengthen decays, and gently bias gain toward low modes, which preserves the existing family architecture and deterministic excitation path while making size-direction tests meaningful across all object families.
- 2026-05-01: Implemented rust as a shared `RustAmount` transform in `src/renderer.rs` that runs after size scaling but before second-order mode construction. That keeps corrosion behavior additive and data-driven across pipe, plate, and tank while making the intended Gate 0 direction—darker highs and shorter tails—easy to verify through both mode metrics and offline render metrics.
- 2026-05-01: Implemented damage as a new shared `DamageAmount` transform in `src/renderer.rs` that runs alongside size and rust before second-order mode construction. Instead of a family-specific branch, the transform deterministically detunes each base mode and adds a shorter-lived companion mode, which produces rougher/beating behavior while preserving the existing excitation path and profile architecture.
- 2026-05-01: Added a `RealtimeModeCountEstimate` seam in `src/renderer.rs` and fixed the conservative real-time budget to the canonical family tables—pipe 6, plate 8, tank 8, with a shared safe cap of 8 modes per voice. The code also records the current offline peak counts after full damage expansion—12/16/16—so later plugin work can start from the safe budget without losing sight of the richer offline ceiling.
- 2026-05-01: Recorded the CPU hot-spot guidance in notepads instead of changing the DSP path. The concrete hot sections in `src/renderer.rs` are (1) the per-frame render loop and its nested per-mode `SecondOrderMode::process` calls (`render_with_excitation` → `PlaceholderResonator::process_sample`), (2) parameter/build-time mode expansion and allocation in `ModalProfile::scaled_mode_specs`, `ModalModeSpec::damaged`, and `PlaceholderResonator::from_profile`, and (3) offline-only summary bookkeeping (`peak`, RMS energy, checksum, and output buffering) performed inside the same sample loop.
- 2026-05-01: Chose a dedicated `render_family_comparisons_to_dir(...)` path plus a minimal built-in PCM WAV writer instead of adding dependencies or creating a separate export script. The new path reuses the current deterministic excitation and `PlaceholderResonator::with_profile(...)` seam for pipe, plate, and tank while emitting a simple manifest and per-family summaries for later evidence collection.
- 2026-05-01: For the rust-evidence step, kept the CLI focused on a dedicated `render_rust_variations_to_dir(...)` export under `output/rust-variations/` and used the pipe profile as the reference object with `low_rust` (`0.25`) and `high_rust` (`0.85`) variants. Pipe already has the clearest existing rust tests, so this keeps the evidence set straightforward while still exercising the shared rust transform rather than introducing a rust-only DSP branch.

- 2026-05-01: For the damage-evidence step, switched the CLI entry to a dedicated `render_damage_variations_to_dir(...)` export under `output/damage-variations/` while leaving the earlier rust renderer path intact inside `src/renderer.rs`. The new evidence set also uses the pipe profile with `low_damage` (`0.25`) and `high_damage` (`0.85`) variants because the existing damage tests and summaries already use pipe as the clearest reference object for hearing roughness and tail changes through the shared transform.
